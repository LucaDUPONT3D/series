{% extends 'base.html.twig' %}

{% block title %}
{{ parent() }} | Add
{% endblock %}

{% block body %}
    <h2>Add New Serie</h2>

    {{ form_start(serieForm) }}

    {{ form_widget(serieForm) }}
    <button>Add</button>

    {{ form_end(serieForm)}}
{% endblock %}

